/**
 * @deprecated The python-rag-service has been deprecated.
 * Document ingestion and search retrieval are now handled natively inside Next.js
 * using Pinecone's hosted inference embeddings. See `@/lib/pinecone` and `@/lib/parser`.
 */
export {};
